# --- Firmware / Pioneer artifacts (never commit) -------------------------
*.UPD
*.upd
*.iso
*.bin
*.mot
*.cramfs
*.tar.gz
*.tar.bz2
*.tar.xz

# Firmware decryption key (user-supplied, never commit)
keys/*.key
keys/*.bin

# Extracted firmware tree (you generate this locally)
/extracted/*
!/extracted/README.md

# Pioneer / Denon binaries
rbp
rbp-*
!tools/patch-rbp/**

# --- Built shims / objects -------------------------------------------------
*.o
*.so
*.so.*
*.a
*.elf

# --- Rust ------------------------------------------------------------------
target/

# --- OS / editor junk ------------------------------------------------------
.DS_Store
Thumbs.db
*.swp
*~
